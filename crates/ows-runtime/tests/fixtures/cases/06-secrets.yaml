name: declared secrets resolve into $secrets
workflow:
  document:
    dsl: '1.0.3'
    namespace: cases
    name: secrets
    version: '0.1.0'
  use:
    secrets: [token]
  do:
    - s:
        set:
          token: '${ $secrets.token }'
secrets:
  token: abc
expect:
  output:
    token: abc
