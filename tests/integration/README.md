# Integration

Complete-workflow execution tests live primarily in `crates/*/tests/` (e.g.
`crates/ows-runtime/tests/tasks.rs`, `basic.rs`, `errors.rs`). This directory
may hold additional fixtures and scenarios for full workflow executions against
sample OWS definitions.
